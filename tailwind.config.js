module.exports = {
  content: [
    "./*.php",        // Include all PHP files in the root directory
    "./**/*.php",     // Include all PHP files in subdirectories
    "./*.html",       // Include HTML files if needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
